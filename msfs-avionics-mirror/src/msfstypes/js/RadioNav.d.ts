declare namespace RadioNav {
  function isHz833Compliant(_MHz: number): boolean;
  function isHz25Compliant(_MHz: number): boolean;
  function isHz50Compliant(_MHz: number): boolean;
  function isXPDRCompliant(_code: number): boolean;
}
